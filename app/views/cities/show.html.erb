<% content_for :head do %>
	<%= stylesheet_link_tag 'cities' %>
	<%= stylesheet_link_tag 'buildings' %>
<% end %>

<% provide(:title, @city.name) %>


<div class="bg-texture-wrapper">
	<!-- Controls which view is rendered depending on if the city has buildings or not -->
	<%= render_city(@buildings) %>
</div>
	