<% content_for :head do %>
	<%= stylesheet_link_tag 'profile' %>
<% end %>

<% provide(:title, 'My Account') %>

<div class="container">
	
	<%= render 'profile_header' %>

	<div class="profile-content-area">
		
		<div class="move-placeholder">
			<h3>You have not written a review yet</h3>
			<%= link_to(raw("<button class=\"btn btn-primary btn-large\">Write a review</button>"), new_review_path) %>
		</div>
	</div>
</div>
